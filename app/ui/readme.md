# `app/ui`

> Contains all the UI components for your application, such as cards, tables, and
  forms.

This learning project proveded by NextJS tutorial, the project already build using
pre-define style for the components.

# Chapter 2 - CSS Styling

- `glocal.css` global css file used for entire application
  - placement -> good placed at top-level component (root-layout -`app/layout.tsx`)

The project comes with TailwindCSS, and the `@tailwind` CSS directive seted up 
on `global.css`.

example pre-installed tailwindcss class: `app/page.tsx`